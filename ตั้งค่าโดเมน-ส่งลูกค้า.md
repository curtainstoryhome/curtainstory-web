# ตั้งค่าโดเมน curtainstoryhome.com ให้ชี้มาที่เว็บ

เอกสารนี้ส่งให้เจ้าของร้านทำเองได้ ใช้เวลาประมาณ 5 นาที
ทำครั้งเดียวจบ ไม่ต้องทำอีก

---

## ก่อนเริ่ม

ต้องมี: บัญชี Hostinger ที่ซื้อโดเมนไว้ (อีเมลกับรหัสผ่านของร้าน)

**ยังไม่ต้องทำอะไรกับเว็บไซต์ในโฮสติ้ง** — เว็บของเราไม่ได้อยู่ที่นั่น
ถ้า Hostinger ชวนสร้างเว็บหรือติดตั้ง WordPress ให้**ข้ามทั้งหมด**
สร้างไปจะกลายเป็นเว็บซ้ำมาแย่งอันดับใน Google กันเอง

---

## ขั้นที่ 1 — เปิดหน้าตั้งค่า DNS

1. เข้า hpanel.hostinger.com แล้วเข้าสู่ระบบ
2. เมนูซ้าย เลือก **โดเมน** → เลือก `curtainstoryhome.com`
3. เลือก **DNS / Nameservers** → แท็บ **DNS Records** (หรือ "จัดการระเบียน DNS")

---

## ขั้นที่ 2 — ลบระเบียนเดิมที่ชนกัน

ถ้ามีบรรทัดที่ **Type** เป็น `A` และ **Name** เป็น `@`
หรือ **Type** เป็น `CNAME` และ **Name** เป็น `www` อยู่แล้ว → **ลบทิ้งก่อน**

ระเบียนพวกนี้ชี้ไปที่โฮสติ้งของ Hostinger ถ้าไม่ลบ เว็บจะเข้าไม่ถูกที่

> ห้ามลบระเบียนที่เป็น `MX` หรือ `TXT` — พวกนั้นเป็นของอีเมล ลบแล้วอีเมลจะเสีย

---

## ขั้นที่ 3 — เพิ่ม 2 บรรทัดนี้

**บรรทัดที่ 1**

| ช่อง | ใส่ |
| --- | --- |
| Type | `A` |
| Name | `@` |
| Points to / Content | `76.76.21.21` |
| TTL | ปล่อยค่าเดิม |

**บรรทัดที่ 2**

| ช่อง | ใส่ |
| --- | --- |
| Type | `CNAME` |
| Name | `www` |
| Points to / Content | `cname.vercel-dns.com` |
| TTL | ปล่อยค่าเดิม |

กด **บันทึก / Add Record** ทั้งสองบรรทัด

---

## ขั้นที่ 4 — แจ้งผู้ทำเว็บ

บอกว่า **"ตั้ง DNS เรียบร้อยแล้ว"** จากนั้นผู้ทำเว็บจะผูกโดเมนเข้าระบบ
และออกใบรับรองความปลอดภัย (กุญแจล็อก https) ให้อัตโนมัติ

DNS ใช้เวลากระจายทั่วโลกประมาณ **10 นาที ถึง 2 ชั่วโมง**
ระหว่างนั้นเปิดเว็บอาจยังไม่ขึ้น เป็นเรื่องปกติ ไม่ต้องแก้อะไรเพิ่ม

---

## ตรวจว่าสำเร็จ

เปิดทั้งสองอย่างนี้แล้วต้องเจอเว็บ CURTAIN STORY:

- `curtainstoryhome.com`
- `www.curtainstoryhome.com`

และหน้าเว็บต้องขึ้น **รูปกุญแจล็อก** ข้างที่อยู่ (https)

---

## สำคัญ หลังทำเสร็จ

- **เปิดต่ออายุอัตโนมัติของโดเมน** — ถ้าปล่อยหมดอายุ เว็บดับทันที
  และคนอื่นอาจจดชื่อนี้ไปได้
- **เก็บอีเมลและรหัสผ่านบัญชี Hostinger ไว้ให้ดี** ใบแจ้งต่ออายุส่งไปที่นั่น
- **อย่าส่งรหัสผ่านทางแชท**
