# ย้ายระบบทั้งหมดไปเป็นของเจ้าของร้าน

ตอนนี้เว็บและฐานข้อมูลอยู่ในบัญชีของผู้ทำเว็บ เอกสารนี้ย้ายทั้งสองอย่าง
ไปอยู่ในบัญชีของร้าน โดยผู้ทำเว็บยังเข้าไปดูแลได้เหมือนเดิม

**เว็บไม่ดับระหว่างย้าย** และ **ไม่ต้องแก้โค้ดแม้แต่บรรทัดเดียว**
เพราะรหัสโครงการและคีย์ทั้งหมดติดไปกับโปรเจกต์ ไม่ได้เปลี่ยน

---

## ทำไมต้องย้าย

ถ้าเว็บอยู่ในบัญชีส่วนตัวของผู้ทำเว็บ วันหนึ่งที่เลิกทำงานด้วยกัน
ร้านจะมีโดเมนแต่ไม่มีเว็บ และถ้าผู้ทำเว็บเข้าบัญชีตัวเองไม่ได้
เว็บร้านก็ดับไปด้วย ย้ายแล้วหมดปัญหาทั้งสองทาง

---

## ลำดับสำคัญ — ย้ายก่อน ค่อยผูกโดเมน

ถ้าผูกโดเมนไปแล้วค่อยย้าย จะต้องมาตั้งโดเมนใหม่อีกรอบ
**ทำตามลำดับ 1 → 2 → 3 → 4 เท่านั้น**

---

## ขั้นที่ 1 — เจ้าของร้านสมัคร 2 บัญชี (ฟรีทั้งคู่)

ใช้ **อีเมลของร้าน** ทั้งสองที่ ไม่ใช่อีเมลผู้ทำเว็บ

| บริการ | สมัครที่ | แพ็กเกจ |
| --- | --- | --- |
| Vercel (ตัวเว็บ) | vercel.com/signup | Hobby — ฟรี |
| Supabase (ข้อมูล+รูป) | supabase.com/dashboard/sign-up | Free |

สมัครเสร็จ **เปิด 2FA ทั้งสองบัญชี** และเก็บรหัสสำรองไว้ที่ปลอดภัย
บัญชีสองอันนี้คือเว็บของร้านทั้งหมด

---

## ขั้นที่ 2 — เชิญผู้ทำเว็บเข้าไปเป็นผู้ร่วมดูแล

ต้องทำก่อนโอน ไม่งั้นโอนไม่ได้ (ระบบบังคับว่าคนโอนต้องอยู่ทั้งสองฝั่ง)

**Vercel** → Settings → Members → Invite → ใส่อีเมลผู้ทำเว็บ → บทบาท **Member**

**Supabase** → เลือกองค์กร → Team → Invite → ใส่อีเมลผู้ทำเว็บ → บทบาท **Owner**
(Supabase ต้องเป็น Owner ถึงจะรับโอนโปรเจกต์ได้ ลดเป็น Administrator ทีหลังได้)

---

## ขั้นที่ 3 — ผู้ทำเว็บกดโอนโปรเจกต์

**Supabase**
1. เปิดโปรเจกต์ `buitincurtains` → Project Settings → General
2. เลื่อนลงล่างสุด → **Transfer project**
3. เลือกองค์กรของร้าน → ยืนยัน

> รหัสโครงการ, ที่อยู่ฐานข้อมูล และคีย์ **ไม่เปลี่ยน** เว็บจึงทำงานต่อได้ทันที

**Vercel**
1. เปิดโปรเจกต์ `buitincurtains` → Settings → Advanced
2. **Transfer Project** → เลือกบัญชีของร้าน → ยืนยัน
3. ฝั่งร้านกดรับโอน

> ค่าตั้งค่า (Environment Variables) ย้ายไปด้วย
> แต่ที่อยู่ชั่วคราว `buitincurtains.vercel.app` จะเปลี่ยน — ไม่เป็นไร
> เพราะกำลังจะใช้โดเมนจริงอยู่แล้ว

---

## ขั้นที่ 4 — ผูกโดเมนจริง

หลังย้ายเสร็จค่อยผูก `curtainstoryhome.com` ในบัญชี Vercel ของร้าน
(ดูขั้นตอนใน `ตั้งค่าโดเมน-ส่งลูกค้า.md`)

---

## ตรวจว่าย้ายครบ — เทียบตัวเลขนี้

หลังย้าย เปิด Supabase → Table Editor แล้วนับให้ตรงกับตารางนี้
ถ้าตัวเลขไหนไม่ตรง **อย่าเพิ่งลบของเดิม** ให้แจ้งผู้ทำเว็บก่อน

| ตาราง | จำนวนแถวที่ต้องมี |
| --- | --- |
| `projects` | 8 |
| `project_images` | 69 |
| `services` | 7 |
| `service_images` | 45 |
| `hero_images` | 3 |
| `review_images` | 3 |
| `why_us_items` | 5 |
| `site_settings` | 53 |
| `business_info` | 1 |

*(นับ ณ วันที่ทำเอกสารนี้ ถ้ามีการเพิ่มผลงานใหม่ ตัวเลขจะมากกว่านี้ได้)*

**ที่เก็บรูป (Storage) ว่างเปล่า 0 ไฟล์** เพราะรูปทั้งหมดอยู่ในโค้ด
ไม่มีไฟล์ต้องขนย้าย

### ตรวจหน้าเว็บจริงอีกรอบ

- เปิดหน้าแรก → รูปขึ้นครบ
- เปิดหน้าผลงาน → เห็น 8 งาน ทุกใบมีรูป
- เข้า `/admin` → ล็อกอินได้ → ลองแก้ข้อความ 1 จุดแล้วกดบันทึก → เว็บจริงเปลี่ยน
- เปิด `/api/keep-alive` → ต้องได้ `{"ok":true}`

---

## ค่าตั้งค่าที่ต้องมีครบใน Vercel

ถ้าหลังย้ายแล้วเว็บ build ไม่ผ่าน ให้เช็คว่ามีครบ 2 ตัวนี้
(Settings → Environment Variables)

| ชื่อ | จำเป็น |
| --- | --- |
| `NEXT_PUBLIC_SUPABASE_URL` | ใช่ |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | ใช่ |
| `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` | เฉพาะตอนยื่น Google Search Console |
| `NEXT_PUBLIC_SITE_URL` | ไม่ต้องใส่ — ระบบอ่านโดเมนจาก Vercel เอง |

**ห้ามส่งค่าพวกนี้ทางแชท** ให้ก๊อบจากหน้าเดิมไปวางหน้าใหม่โดยตรง

---

## หลังย้ายเสร็จ ใครถืออะไร

| ส่วน | เจ้าของ |
| --- | --- |
| โดเมน | ร้าน |
| Vercel (ตัวเว็บ) | **ร้าน** |
| Supabase (ข้อมูล+รูป) | **ร้าน** |
| Google Business Profile · LINE OA · Facebook | ร้าน |
| ผู้ทำเว็บ | เป็นผู้ร่วมดูแล ถอนสิทธิ์ได้ทุกเมื่อ |

ถ้าวันหนึ่งเปลี่ยนผู้ดูแล ร้านแค่ถอนสิทธิ์คนเก่าแล้วเชิญคนใหม่
**เว็บไม่ต้องย้าย ไม่ดับ ไม่เสียข้อมูล**
