# ย้ายระบบทั้งหมดไปเป็นของเจ้าของร้าน

ตอนนี้เว็บและฐานข้อมูลอยู่ในบัญชีของผู้ทำเว็บ เอกสารนี้ย้ายทั้งสองอย่าง
ไปอยู่ในบัญชีของร้าน โดยผู้ทำเว็บยังเข้าไปดูแลได้เหมือนเดิม

**เว็บไม่ดับระหว่างย้าย** และ **ไม่ต้องแก้โค้ดแม้แต่บรรทัดเดียว**
เพราะรหัสโครงการและคีย์ทั้งหมดติดไปกับโปรเจกต์ ไม่ได้เปลี่ยน

---

## ทำไมต้องย้าย

ถ้าเว็บอยู่ในบัญชีส่วนตัวของผู้ทำเว็บ วันหนึ่งที่เลิกทำงานด้วยกัน
ร้านจะมีโดเมนแต่ไม่มีเว็บ และถ้าผู้ทำเว็บเข้าบัญชีตัวเองไม่ได้
เว็บร้านก็ดับไปด้วย ย้ายแล้วหมดปัญหาทั้งสองทาง

---

## ลำดับสำคัญ — ย้ายก่อน ค่อยผูกโดเมน

ถ้าผูกโดเมนไปแล้วค่อยย้าย จะต้องมาตั้งโดเมนใหม่อีกรอบ
**ทำตามลำดับ 1 → 2 → 3 → 4 เท่านั้น**

---

## ขั้นที่ 1 — เจ้าของร้านสมัคร 2 บัญชี (ฟรีทั้งคู่)

ใช้ **อีเมลของร้าน** ทั้งสองที่ ไม่ใช่อีเมลผู้ทำเว็บ

| บริการ | สมัครที่ | แพ็กเกจ |
| --- | --- | --- |
| Vercel (ตัวเว็บ) | vercel.com/signup | Hobby — ฟรี |
| Supabase (ข้อมูล+รูป) | supabase.com/dashboard/sign-up | Free |

สมัครเสร็จ **เปิด 2FA ทั้งสองบัญชี** และเก็บรหัสสำรองไว้ที่ปลอดภัย
บัญชีสองอันนี้คือเว็บของร้านทั้งหมด

---

## ขั้นที่ 2 — เอาซอร์สโค้ดขึ้น Git ก่อน (สำคัญที่สุด)

ตรวจแล้วพบว่า **ซอร์สโค้ดมีอยู่ที่เดียวคือเครื่องผู้ทำเว็บ** และ deploy
ที่ผ่านมาคืออัปโหลดไฟล์ตรงจาก CLI ทุกครั้ง ถ้าเครื่องนั้นเสีย โค้ดหายถาวร
และส่งมอบให้ร้านก็ไม่ได้เพราะไม่มีที่ให้ดึง

สร้าง repo ในเครื่องและ commit แรกไว้แล้ว (236 ไฟล์, ไม่มีไฟล์ความลับ)
เหลือแค่ push ขึ้น GitHub

1. สร้าง repo ใหม่บน GitHub — **ตั้งเป็น Private**
2. ในโฟลเดอร์โปรเจกต์ รันสองคำสั่งนี้ (เปลี่ยน URL เป็นของ repo ที่สร้าง)

```bash
git remote add origin https://github.com/<เจ้าของ>/<ชื่อ-repo>.git
git push -u origin master
```

3. ถ้าจะให้เป็นของร้านจริง → GitHub → Settings → **Transfer ownership**
   ไปที่บัญชีของร้าน แล้วให้ร้านเชิญผู้ทำเว็บกลับเข้าเป็น Collaborator

---

## ขั้นที่ 3 — เชื่อม Vercel ของร้านเข้ากับ Git

**Vercel Hobby โอนโปรเจกต์ข้ามบัญชีและเชิญผู้ร่วมดูแลไม่ได้** — เอกสารของ
Vercel ระบุว่า Team collaboration มีเฉพาะแพ็กเกจ Pro ฉะนั้นไม่ใช้วิธีโอน
ให้ deploy ใหม่จาก Git เข้าบัญชีร้านตรงๆ ซึ่งได้ผลเหมือนกันและสะอาดกว่า

1. บัญชี Vercel ของร้าน → **Add New → Project → Import** เลือก repo
2. ใส่ค่าตั้งค่า 2 ตัว (ก๊อบจากโปรเจกต์เดิม **ห้ามส่งทางแชท**)
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
3. Deploy

**ผลพลอยได้ที่ดีมาก:** หลังจากนี้ผู้ทำเว็บแก้โค้ดแล้ว `git push`
เว็บอัปเดตเอง **ไม่ต้องมีสิทธิ์ใน Vercel ของร้านเลย** เข้าแดชบอร์ดเฉพาะตอน
ตั้งโดเมนหรือแก้ค่าตั้งค่า ซึ่งนานๆ ครั้ง

**Supabase** โอนได้ปกติ (ไม่มีข้อจำกัดแบบ Vercel)
1. ร้านเชิญผู้ทำเว็บเข้าองค์กร บทบาท **Owner**
2. ผู้ทำเว็บ → โปรเจกต์ `buitincurtains` → Project Settings → General →
   **Transfer project** → เลือกองค์กรของร้าน
3. รหัสโครงการและคีย์ **ไม่เปลี่ยน** เว็บทำงานต่อได้ทันที

---

## ข้อควรรู้เรื่องแพ็กเกจ Vercel

เอกสาร Vercel ระบุว่า *"the Hobby plan restricts users to non-commercial,
personal use only"* — เว็บร้านค้าถือเป็นการใช้เชิงพาณิชย์ ทางเลือกคือ

| ทาง | ค่าใช้จ่าย | หมายเหตุ |
| --- | --- | --- |
| อยู่ Hobby ต่อ | ฟรี | ผิดเงื่อนไข ความเสี่ยงจริงคือ Vercel ส่งเมลให้อัปเกรด ถ้าไม่ตอบอาจถูกระงับ |
| Vercel Pro | ~$20/เดือน (~฿700) | ถูกต้องตามเงื่อนไข + เชิญผู้ร่วมดูแลได้ |
| ย้ายไป Cloudflare Pages หรือ Netlify | ฟรี | แพ็กเกจฟรีอนุญาตใช้เชิงพาณิชย์ แต่ต้องมีงานย้ายและทดสอบใหม่ |

เว็บนี้ traffic ต่ำ ความเสี่ยงถูกบังคับอัปเกรดไม่สูง แต่ควรเป็นการ
**ตัดสินใจโดยรู้ตัว** ไม่ใช่ปล่อยไปโดยไม่รู้

---

## ขั้นที่ 4 — ผูกโดเมนจริง

หลังย้ายเสร็จค่อยผูก `curtainstoryhome.com` ในบัญชี Vercel ของร้าน
(ดูขั้นตอนใน `ตั้งค่าโดเมน-ส่งลูกค้า.md`)

---

## ตรวจว่าย้ายครบ — เทียบตัวเลขนี้

หลังย้าย เปิด Supabase → Table Editor แล้วนับให้ตรงกับตารางนี้
ถ้าตัวเลขไหนไม่ตรง **อย่าเพิ่งลบของเดิม** ให้แจ้งผู้ทำเว็บก่อน

| ตาราง | จำนวนแถวที่ต้องมี |
| --- | --- |
| `projects` | 8 |
| `project_images` | 69 |
| `services` | 7 |
| `service_images` | 45 |
| `hero_images` | 3 |
| `review_images` | 3 |
| `why_us_items` | 5 |
| `site_settings` | 53 |
| `business_info` | 1 |

*(นับ ณ วันที่ทำเอกสารนี้ ถ้ามีการเพิ่มผลงานใหม่ ตัวเลขจะมากกว่านี้ได้)*

**ที่เก็บรูป (Storage) ว่างเปล่า 0 ไฟล์** เพราะรูปทั้งหมดอยู่ในโค้ด
ไม่มีไฟล์ต้องขนย้าย

### ตรวจหน้าเว็บจริงอีกรอบ

- เปิดหน้าแรก → รูปขึ้นครบ
- เปิดหน้าผลงาน → เห็น 8 งาน ทุกใบมีรูป
- เข้า `/admin` → ล็อกอินได้ → ลองแก้ข้อความ 1 จุดแล้วกดบันทึก → เว็บจริงเปลี่ยน
- เปิด `/api/keep-alive` → ต้องได้ `{"ok":true}`

---

## ค่าตั้งค่าที่ต้องมีครบใน Vercel

ถ้าหลังย้ายแล้วเว็บ build ไม่ผ่าน ให้เช็คว่ามีครบ 2 ตัวนี้
(Settings → Environment Variables)

| ชื่อ | จำเป็น |
| --- | --- |
| `NEXT_PUBLIC_SUPABASE_URL` | ใช่ |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | ใช่ |
| `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` | เฉพาะตอนยื่น Google Search Console |
| `NEXT_PUBLIC_SITE_URL` | ไม่ต้องใส่ — ระบบอ่านโดเมนจาก Vercel เอง |

**ห้ามส่งค่าพวกนี้ทางแชท** ให้ก๊อบจากหน้าเดิมไปวางหน้าใหม่โดยตรง

---

## หลังย้ายเสร็จ ใครถืออะไร

| ส่วน | เจ้าของ |
| --- | --- |
| โดเมน | ร้าน |
| Vercel (ตัวเว็บ) | **ร้าน** |
| Supabase (ข้อมูล+รูป) | **ร้าน** |
| Google Business Profile · LINE OA · Facebook | ร้าน |
| ผู้ทำเว็บ | เป็นผู้ร่วมดูแล ถอนสิทธิ์ได้ทุกเมื่อ |

ถ้าวันหนึ่งเปลี่ยนผู้ดูแล ร้านแค่ถอนสิทธิ์คนเก่าแล้วเชิญคนใหม่
**เว็บไม่ต้องย้าย ไม่ดับ ไม่เสียข้อมูล**
